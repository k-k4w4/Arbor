import SwiftUI

// Phase 3: Commit list implementation
struct CommitListView: View {
    var body: some View {
        Text("コミットリスト")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .foregroundStyle(.secondary)
    }
}
