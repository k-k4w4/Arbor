import SwiftUI

// Phase 4: Commit detail panel
struct DetailView: View {
    var body: some View {
        EmptyStateView(
            icon: "cursorarrow.click",
            message: "コミットを選択してください"
        )
    }
}
